server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA フォールバック: 存在しないパスはすべて index.html へ
    # /api/* へのルーティングは本番環境では ALB が担うため、ここでの proxy_pass は不要
    location / {
        try_files $uri $uri/ /index.html;
    }
}
